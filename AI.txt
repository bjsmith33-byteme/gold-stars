# AI Assistant Notes for this repo
## Approximate percentage code by AI: 98%

## Design decisions (initial build prior to tracking)
Key choices behind the public "Gold Stars" site and why:

- This project started to take form early in the semester with an employer-internal site
  that I started working on at the same time as coming up with ideas for the web project.
  This file documents the decisions that were made prior to its creation and then further
  documents the decisions, additions, deletions, and edits, primarly made with Claude code
  for all functional changes until early-mid July, where these are tracked more discretely.
- Plain JavaScript / JSX (no TypeScript) — kept the code easy to read and hand-edit.
  SUPERSEDED 2026-08-01: converted to TypeScript so this repo and the private predecessor
  run the SAME source files, differing only in src/config/. Readability held up: the
  types mostly annotate the CSV row shape and the config contract, which is where the
  hand-editing questions actually come up.
- React-Bootstrap (Bootstrap 5.3) — responsive/mobile-friendly out of the box; its
  Offcanvas powers the side-nav ("sheet" on mobile).
- React Router in declarative mode with HashRouter — GitHub Pages has no single-page-app
  fallback, so hash routes (#/kb, #/about) work without any server config or 404 redirect.
- Data is a single hand-editable CSV (public/gold-stars.csv) read live at runtime — no
  backend or database. Knowledge areas are React / JavaScript / CSS / SwiftUI; the roster
  is ~20 diverse fake names in src/lib/roster.js. All sample content is dummy data.
- One unified leaderboard (no team/sub-group split) to keep the public version simple.
  As of 2026-08-01 this is a CONFIG value (roles.podRoles: []) rather than missing code —
  a team that wants the split turns it on without touching a component.
- The "role" field is just a cosmetic specialty badge (Frontend/Backend/Mobile/Full-stack);
  it doesn't gate anything yet.
- Nominations are email-only (a mailto: from the Award form). A static public site has no
  backend and shouldn't expose direct data access, so a human reviews before adding a row.
  (Previously had allowed direct access to the csv for group members, but this version needs
  to be public.)
- Deploy model: build to docs/ with base "/gold-stars/"; GitHub Pages serves from /docs.
  Building does NOT publish — the site changes only when docs/ is committed and pushed
  (intentional, for control over when changes go live). No CI.
- Light/dark via Bootstrap's data-bs-theme, with a pre-paint script to avoid a flash.
- Contact/nomination email, the roster, and the CSV rows are PLACEHOLDERS to be replaced.

Which details to show, and how:
- One "gold star" per helpful contribution. The board shows the recipient, their
  role/specialty, the knowledge area, and a star count — recognition, never criticism.
- Standard competition ranking (ties share a place), tie-aware winners, and medals.
- The nominator (who gave the kudos) is stored but NOT shown on the board — it stays
  behind the scenes and instead powers a "Supporter of the Month" (top kudos-giver).
- Alongside the leaderboard: monthly winners, a per-knowledge-area breakdown to surface
  the growing expert in each area, a personal "My Stats" view (total, rank, a month-by-
  month trend, area mix, recent stars), and a Knowledge Base.
- Self-answers count if the person did the research; good questions are encouraged too.

How data is stored:
- A single flat file (CSV), one row per award, read live at view time — no backend or
  database. Columns: date, recipient, role, category, note, source, nominator, sub-topic.
- The note field does double duty: any row with a note becomes a Knowledge Base entry
  (problem -> solution), so the board and the KB share one source of truth.
- Chose a plain, hand-editable, version-controlled file for simplicity and transparency.

How data is collected:
- Primary: a web form that composes a row and hands it off by email, so a human reviews
  it before it's added — no direct write access to the data needed.
- Also explored auto-collecting stars from team-chat messages (a message with a star and
  who it's for). Lesson learned: the chat platform's API returned only ONE reaction per
  message, so reading emoji *reactions* was unworkable — the design switched to parsing
  message *text + mentions*, pulled on a schedule, with a watchdog to alert on failed runs.
  UPDATED 2026-08-01: the message *parser* is now included here (src/lib/teams.ts) and
  tested, but ships SWITCHED OFF behind features.chatComposer with a placeholder chat
  block — no real chat ids, and the scheduled fetcher/committer is not included. It's here
  so the template is complete for a team that wants to wire up their own chat platform.

Future ideas discussed (not all built):
- Recognize learners, not just answerers — a "Learner of the Month" for the best
  question-askers (needs capturing who asked).
- Optional email-on-award to the recipient and/or their manager/lead.
- A dedicated, searchable Knowledge Base page (built in this public version).
- A curated "Question of the Month"; an automated summarizer to turn raw Q&A into clean
  KB notes; a personal "curiosity" stat (questions asked that became KB entries).

Ideas considered and rejected (and why):
- A wiki-style page — no dynamic data or aggregation.
- A reporting/BI summary page — not self-service; needed platform-team setup.
- Reading chat emoji reactions via the platform API — only one reaction per message.
- A popular test runner whose binary was blocked in the environment — used the language's
  built-in test runner instead.
- For this public version specifically: dropped the team/sub-group ("Friends") split for
  simplicity, and dropped chat + direct-file editing in favor of email-only nominations.

## Task log - About 99% Claude doing the coding under my direction
Format: YYYY-MM-DD  <task summary>

2026-07-03  Initial build: created this public, generic "Gold Stars" recognition app
            from a private predecessor. Stack: Vite + React (plain JSX, no TypeScript) +
            React-Bootstrap + React Router (declarative, HashRouter). Features: all-time
            leaderboard, monthly winners, Supporter of the Month, By-Knowledge-Area
            (React/JavaScript/CSS/SwiftUI), a searchable Knowledge Base on its own page,
            My Stats, an offcanvas side-nav, light/dark theme, and email-only nominations.
            Data is a hand-editable dummy public/gold-stars.csv (~20 fake names). Builds to
            docs/ for GitHub Pages (base /gold-stars/).
2026-07-03  Added a star favicon; capped the leaderboard to the top 4 places (top 3 if
            4th place is tied); fixed contrast (neutral navbar so the star is visible,
            bolder winner-banner text, outlined the Award button's star).
2026-07-03  Scoped the leaderboard top-4 cap to the all-time board only (monthly boards
            now show everyone); added more React tips to the knowledge base.
2026-07-03  Aligned the nav menu with its button — changed the Offcanvas placement to
            "end" so it opens from the right, under the hamburger toggle.
2026-07-03  Added a "Background: design history" section documenting the predecessor's
            decisions (what to show, how to store/collect data) plus future & rejected
            ideas — sanitized (no internal names, IDs, product areas, or tooling).
2026-07-04  Added opt-in "Preview changes" mode: stage NEW stars locally (browser
            localStorage), preview the board with them merged, edit/remove staged
            entries, and Submit the batch by email for an admin to commit (no server;
            add-only for v1 — editing/deleting published stars is a future v2). Includes
            validation + formula-injection guard, defensive overlay parsing, and a sticky
            "not published" banner. New: src/lib/overlay.js (+ tests), PreviewToggle,
            DraftBanner, ReviewChangesPanel; AwardStarModal now supports a stage/edit mode.
2026-07-05  Visual pass on the theme (src/custom.css). Introduced a --gs-* palette consumed
            by all surfaces so the look is tuned in one place: pale-gold page, white cards,
            soft-gold card/table headers + navbar + footer (all matching), warm-brown
            borders/shadows, faint dividers between rows, de-highlighted specialty badges.
            Warmed the navbar, carded the KB preview, themed the winner (gold) and supporter
            cards. Gold (not gray) buttons in the header/footer. About subtitle set to
            "A lightweight team recognition board." and the Preview toggle hidden on About.
            Set the contact/nomination email to the project placeholder in one shared spot.
2026-07-06  Replaced the static header poster IMAGE with a self-contained React component
            (src/components/StarChartPosterboard.jsx) — inline SVG. Its "ink" (title, names,
            ruled lines) follows the APP theme, not the OS: added src/lib/theme.js
            (useBsTheme, a useSyncExternalStore hook that observes <html data-bs-theme>) so
            it tracks the ThemeToggle. Gold/red/blue stars stay fixed in both themes. Dropped
            the old dark-mode parchment backing (no longer needed) and removed the unused
            public SVG.
2026-07-06  Darkened the dark-mode palette a notch (page/card/header). Changed the My Stats
            time-range pickers and the KB-preview Search button from gray to gold. Made the
            "This Month" section use the same card layout as the past-month cards (winner
            card nested inside, then leaderboard, then supporter), and added left/right edge
            padding to the leaderboard tables so content isn't flush with the card edge.

## Begin tracking manual updates

2026-07-13  String updates (manual)
2026-07-14  Switched to using chart.js react-chartjs-2 for individual stats. Combined approach 
            to displaying metrics with toggle between overall counts OR counts by area and 
            line OR bar charts. Added icons to area line chart for better accessibility. Added 
            jump links from leaderboard to new graphs. (About 25% manual work for charts and 
            data, but then had Claude modularize this component with lazy loading for 
            performance, create unit tests, and incorporate into whole) 
2026-07-15  Styling and string updates. Added hover effect to list group items (manual)
2026-07-20  Tweaking button appearance (manual)
2026-07-21  Rearranged Preview section to use grid (manual)
2026-07-31  Theme updates (manual)
2026-08-01  Modularized the app behind a config seam and converted it to TypeScript
            (Claude, ~100% AI under direction; my part was the decisions below).
            The private predecessor had meanwhile absorbed this repo's UI work — router,
            Preview mode, Chart.js graphs, the Bootstrap look — so it was a strict superset
            of this one. Rather than convert 30 files by hand, I had Claude copy that tree
            over and push every team-specific value into a new src/config/: team.config.ts
            (branding, contacts, roles, areas, sources, feature switches, deploy paths),
            roster.ts, copy.tsx, and types.ts as the documented contract. This repo's
            identity now lives entirely in those four files.
            Decisions I made along the way: keep My Stats chart-only here (the private app
            shows CSS area bars too — that became a features.areaBars flag rather than a
            code fork); include the chat parser but switched off with placeholder ids;
            and treat "one unified board" as config (roles.podRoles: []) instead of
            absent code. Two behavior changes came along with the shared code: Supporter
            of the Month now counts every star in a month, and the sub-topic placeholder
            comes from copy.tsx.
            Tests were rewritten rather than copied — they assert against the LIVE config,
            so e.g. "source: Teams is rejected" is now a true statement about THIS config.
            39 tests, tsc clean, walked the whole app in a browser. docs/ deliberately NOT
            rebuilt, so nothing published yet.
2026-08-10  Published the config-seam work and caught this repo up (Claude under direction).
            The 2026-08-01 branch had sat unpushed for nine days, which turned this into a
            forensic exercise first: the tip commit was titled as if it carried source
            changes but contained only one file re-saved with different line endings, so the
            repo LOOKED a generation behind while the real work sat on an unpushed branch on
            a single machine. Nothing was lost. Lessons I'm keeping: an unpushed branch is
            the actual risk, not drift; and a commit message is not evidence — check what
            the commit changed.
            One genuine leak found before pushing: some of the TypeScript dependencies had
            been installed from an internal package mirror, so package-lock.json recorded
            internal hostnames — which would have published them AND broken a clean install
            for anyone outside. Fixed first. Leak-check BEFORE publishing, and remember the
            lockfile and the built output are part of the published surface, not just src/.
            Then brought over two features that had landed in the predecessor: alumni
            support and a separate non-core-team board on its own /friends page. Both ship
            DORMANT here (empty alumni list, feature flag off) so the template stays simple;
            the tests assert the dormant state, and flipping the config makes them fail,
            which is how I know they're wired to the config rather than vacuous. Added
            SETUP.md — the step-by-step adoption guide the README's table wasn't — plus a
            .gitattributes to stop the line-ending churn that caused the phantom commit, and
            skewed the sample data so it looks like a real board (one area and one person
            dominate) with more beginner-level Q&A.
2026-08-10  Advanced search on the Knowledge Base (Claude under direction). The KB had one
            keyword box and an area dropdown, while the CSV carried four more filterable
            fields nothing could reach, and no search state survived being shared.
            Decisions I made here: put the whole filter core in one dependency-free module
            (src/lib/search.ts) that imports the row shape as a TYPE only, so it never pulls
            the config in at runtime and its tests hold whatever a team configures; derive
            the facet options from the DATA rather than the config, so a configured-but-
            unused area simply isn't offered and the feature needs zero config to adopt; and
            store the selection as EXCLUDED values rather than included ones, so "no filter"
            and "nothing selected" can't be confused, a shared link still means what it said
            after new data arrives, and an unfiltered board is a bare #/kb.
            This is the first code here that WRITES the URL. Both sync directions compare the
            state as the encoder would write it, and because encoding is canonical that
            comparison is self-guarding — no ping-pong between the two effects, and a
            fixed-point test proves it rather than decorating it. Writes are debounced and
            replace history, so Back isn't "undo one keystroke".
2026-08-11  Sub-topics became multi-value tags, and the search learned the team's words
            (Claude under direction; being ported here from the predecessor now).
            This round came out of a usability test. Nine of the ten suggestions were small;
            the tenth — "can an entry have several sub-topics, so they act like keywords?" —
            turned out to be the thing the other search suggestions were waiting on. The
            sub-topic column held ONE value, so an entry could only say it was about one
            thing, and the predecessor's data had already deformed around that: a row tagged
            with a compound label that existed only because it couldn't hold two.
            My call on the modelling question, which I'd been talking myself out of: keep
            sub-topics an exact-match VOCABULARY rather than folding them into the free-text
            search. Folding them in would have collapsed the difference between an entry
            being ABOUT something and merely MENTIONING it, and made the synonym work
            almost worthless. Kept apart, the synonym map is worth MORE than before —
            mapping someone's words onto a tag can now reach an entry whose primary label is
            something else, which is exactly the case a single-value column can never serve.
            Decisions worth recording: the tags are separated by a semicolon, not a comma,
            because the column is LAST in the CSV — a comma needs the cell quoted, and a
            hand-edit that drops the quote silently shifts every column. An entry is FILED
            under its first tag only, so it appears once when browsing and the counts still
            add up; the rest show as cross-references. And the chip row replaced the
            sub-topic dropdown instead of joining it — the previous round left one filter
            with two controls and I didn't want a second.
            Also from the same test: the two halves of the Award form asked for the same
            write-up in two different voices, with different labels, shapes and placeholder
            text, so they're one shared field now; the CSV row preview moved behind the
            existing "Advanced" disclosure (which incidentally fixed a real mismatch — the
            preview didn't match the row either hand-off actually sends); My Stats gained a
            custom date range alongside the quick buttons; and the redundant per-area bars
            came off. That last one was a one-line CONFIG change rather than a deletion,
            because the flag already existed and the bars are still the fallback when charts
            are switched off — the seam paying for itself.
            One bug the tests didn't catch and the browser did: typing "A, B" into the new
            tag field produced a single tag called "A B", because the comma was being turned
            into a space instead of the separator. Worth remembering that unit tests covered
            the parser and not the typing.
            124 tests in the predecessor, tsc clean, walked the whole app. docs/ not rebuilt
            here, so nothing is published by this entry.